#include <stdio.h>

#define YES 1
#define NO 0

/* htoi: convert hexdecimal string s to integer */
int htoi(char s[]) {
    int hexdegit, i, inhex, n;

    i = 0;
    if (s[i] == '0') { /* skip optional 0x or 0X */

    }
    return 0;
}

int main() {
    
    return 0;
}